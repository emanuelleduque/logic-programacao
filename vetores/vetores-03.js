// Dado o vetor [15, 5, 8], verifique se o valor na posição 1 é maior do que 10.
let numeros = [15, 5, 8]
console.log("Valor da Primeira Posição: ", numeros[0]);

// Se sim, retornar "Verdadeiro", se não, retornar "Falso".
if (numeros[0]>10){
    console.log("Verdadeiro");
    } else {
    console.log("Falso");
    }